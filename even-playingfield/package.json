{
  "name": "e-pf",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile  --outfile build/epf",
    "config-gen": "bun run --console-depth 4 src/generate-config.ts"
  },
  "bin": "src/index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@openrouter/sdk": "^0.1.11",
    "smol-toml": "^1.5.2",
    "zod-defaults": "^0.2.3"
  }
}
