## Project setup
- This frontend project is using 
  - Svelte 5 with Runes syntax
  - Bulma for styling
- Coding style:
  - TypeScript for type checking
  - Use double quotes for strings
- Use `bun` as the package manager. Install dependencies with `bun install`.
- To start the development server, run `bun run dev`.
